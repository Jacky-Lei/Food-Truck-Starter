<div>Perks: {this.state.foodtruck.perks.map(function (perk){
    {['name', 'description', 'amount'].map(function (attr){
      return <div key={attr}>{attr}: {perk[attr]}</div>;
    })}
}.bind(this)
)}

</div>

<div> Perks: {for (var i = 0; i < perkArray.length; i++){
    return <div>{"Name: " + perkArray[0].name + " Description: " + perkArray[0].description + " Amount: " + perkArray[0].amount}</div>;
  }; }

</div>


logInGuest: function (user) {
  $.ajax({
    url: "/session/",
    method: "POST",
    success: function () {
      redirect
    }
  });
},


<li><a onClick={ApiUtil.logInGuest(user)}>Guest Log In</a></li>

  var user = {username: tester, password: tester};
